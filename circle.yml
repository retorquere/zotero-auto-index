---
machine:
  ruby:
    version: '2.2'
test:
  override:
    - true
general:
  branches:
    ignore:
      - gh-pages
deployment:
  all:
    branch: "/.*/"
    commands:
    - bundle exec rake publish
